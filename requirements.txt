typer[all]>=0.12
rich>=13.0
pydantic>=1.10,<3
python-dateutil>=2.8
apscheduler>=3.10,<4

# Runtime notes:
# - Audio  : winsound (Windows stdlib) — no external dep needed
# - Process: ctypes / os.kill (stdlib)  — no psutil needed
# - Docker : see Dockerfile for Linux sound setup (aplay/afplay)
